version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # Empty message is a valid pattern for RPCs without arguments.
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
    - RPC_REQUEST_RESPONSE_UNIQUE
breaking:
  use:
    - FILE
